{% include 'default/_form.html.twig' with {
    page_title: 'title_event_delete',
    page_title_param: {'%id%': event.id},
    attr: {
        method: 'DELETE',
        action: url('calendar_delete', {'id': event.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'calendar_index',
} %}
