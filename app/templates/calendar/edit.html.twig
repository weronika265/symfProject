{% include 'default/_form.html.twig' with {
    page_title: 'title_event_edit',
    page_title_param: {'%id%': event.id},
    attr: {
        method: 'PUT',
        action: url('calendar_edit', {'id': event.id}),
    },
    form: form,
    back_to_list_path: 'calendar_index',
} %}
