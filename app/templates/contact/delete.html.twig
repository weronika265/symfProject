{% include 'default/_form.html.twig' with {
    page_title: 'title_contact_delete',
    page_title_param: {'%id%': contact.id},
    attr: {
        method: 'DELETE',
        action: url('contact_delete', {'id': contact.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'contact_index',
} %}
