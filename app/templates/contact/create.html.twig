{% include 'default/_form.html.twig' with {
    page_title: 'title_contact_create',
    attr: {
        method: 'POST',
        action: url('contact_create')
    },
    form: form,
    submit_label: 'action_create'|trans,
    back_to_list_path: 'contact_index',
} %}
