{% include 'default/_form.html.twig' with {
    page_title: 'title_tag_delete',
    page_title_param: {'%id%': tag.id},
    attr: {
        method: 'DELETE',
        action: url('tag_delete', {'id': tag.id}),
    },
    form: form,
    submit_label: 'action_delete'|trans,
    back_to_list_path: 'tag_index',
} %}
