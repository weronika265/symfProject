{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_user_details'|trans({'%username%': app.user.username|default('')}) }}
{% endblock %}

{% block body %}
    {% include 'menu.html.twig' %}
    <div class="display">
        <h1>{{ 'title_user_details'|trans({'%username%': app.user.username|default('')}) }}</h1>
        {% if app.user is defined and app.user|length %}
            <dl class="dl-horizontal">
                <dt>{{ 'label_user_username '|trans }}</dt>
                <dd>{{  app.user.username }}</dd>
                <dt>{{ 'label_user_email'|trans }}</dt>
                <dd>{{  app.user.email }}</dd>
{#                <dt>{{ 'label_user_data_first_name'|trans }}</dt>
                <dd>{{  app.user.userData.firstName }}</dd>
                <dt>{{ 'label_user_data_surname'|trans }}</dt>
                <dd>{{  app.user.userData.surname }}</dd>
                <dt>{{ 'label_user_data_data'|trans }}</dt>
                <dd>{{  app.user.userData.data }}</dd>#}
                {# TODO: fix the connection User-userData. #}
            </dl>
            <a href="{{ url('user_edit', {id: user.id}) }}" title="{{ 'action_edit'|trans }}">
                {{ 'action_edit'|trans }}
            </a>
        {% else %}
            <p>
                {{ 'message_item_not_found'|trans }}
                {#<i class="bi bi-arrow-left icon-3x"></i>#}
            </p>
        {% endif %}
    </div>
{% endblock %}
